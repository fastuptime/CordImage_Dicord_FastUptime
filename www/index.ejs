<!DOCTYPE html>
<html lang="tr">
   <head>
      <meta charset="UTF-8">
      <meta http-equiv="X-UA-Compatible" content="IE=edge">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>CordImage - AnaSayfa <%= config.fastuptime %></title>
      <meta name="description" content="CordImage - AnaSayfa <%= config.fastuptime %>">
      <meta name="keywords" content="CordImage - AnaSayfa <%= config.fastuptime %>">
      <meta name="author" content="CordImage - AnaSayfa <%= config.fastuptime %>">
   </head>
   <body>
      <h1 style="text-align: center; margin-top: 50px; color: rgb(255, 255, 255); font-size: 50px;">CordImage</h1>
      <form action="/upload" method="post" enctype="multipart/form-data">
         <input type="file" name="dosya"  id="dosya" />
         <p id="yukleniyor">CordImage - Resim Seçiniz</p>
         <button type="submit" id="gonder" style="display: none;">Yükle</button>
      </form>
      <style>
         body{
         background: rgba(0,0,0,0.9);
         }
         form{
         position: absolute;
         top: 50%;
         left: 50%;
         margin-top: -100px;
         margin-left: -250px;
         width: 500px;
         height: 200px;
         border: 4px dashed #fff;
         }
         form p{
         width: 100%;
         height: 100%;
         text-align: center;
         line-height: 170px;
         color: #ffffff;
         font-family: Arial;
         }
         form input{
         position: absolute;
         margin: 0;
         padding: 0;
         width: 100%;
         height: 100%;
         outline: none;
         opacity: 0;
         }
         form button{
         margin: 0;
         color: #fff;
         background: #16a085;
         border: none;
         width: 508px;
         height: 35px;
         margin-top: -20px;
         margin-left: -4px;
         border-radius: 4px;
         border-bottom: 4px solid #117A60;
         transition: all .2s ease;
         outline: none;
         }
         form button:hover{
         background: #149174;
         color: #0C5645;
         }
         form button:active{
         border:0;
         }
         
        button.disabled {
          opacity: 0.5;
          cursor: not-allowed;
          background-color: #ccc;
        }
      </style>
    
      
      <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script> 
      <script>
         const input = document.getElementById("dosya");
         const yukleniyor = document.getElementById("yukleniyor");
         const gonder = document.getElementById("gonder");
        input.addEventListener("change", function(){
            if(this.files && this.files[0]){
            const dosya = new FileReader();
            dosya.onload = function(e){
                yukleniyor.innerHTML = `<img src="${e.target.result}" width="500" height="200" />`;
                gonder.style.display = "block";
            }
            dosya.readAsDataURL(this.files[0]);
            }
        });
      </script>
   </body>
</html>

<!--

    Author: ByCan
    Github: github.com/fastuptime
    Website: fastuptime.com
    Product: CordImage
    
-->